
#include<iostream>
using namespace std;
int main(){
    float a, b, c;
    cin >> a >> b >> c;                         // سه عدد float می‌گیرد
    cout << "Average = " << (a + b + c) / 3;   // میانگین = جمع تقسیم بر ۳
}
