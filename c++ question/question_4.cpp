#include<iostream>
using namespace std;
int main(){
    int a;
    cin >> a;                    // ضلع را می‌گیرد
    int p = 4 * a;               // محیط = ۴ × ضلع
    cout << "Perimeter = " << p << ", Double perimeter = " << 2*p;
}
