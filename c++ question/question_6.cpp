
#include<iostream>
#include<cmath>
using namespace std;
int main(){
    int base, exp;
    cin >> base >> exp;                     // مبنا و توان می‌گیرد
    cout << pow(base, exp);                 // محاسبه توان با pow
}
