#include<iostream>
using namespace std;
int main(){
    int age;
    cin >> age;                                     // سن می‌گیرد
    if(age >= 18) cout << "You can drive";           // اگر ۱۸ یا بالاتر بود
    else cout << "You cannot drive";
}
